import React, { useContext } from 'react'
import GlobalContext from '../../contexts/GlobalContext'
export default function MainView () {

  // const [requests, state, dispatch, actions] = useContext(GlobalContext)
  return (
    <div className="mainView">
      MAIN view
      {/* {state} */}
    </div>
  )
}